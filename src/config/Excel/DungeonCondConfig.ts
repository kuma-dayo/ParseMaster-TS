import { ConfigDefault } from "../"

export const DungeonCondConfig: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    condType: "DungeonCondType",
    param: "vint[]",
  },
}
