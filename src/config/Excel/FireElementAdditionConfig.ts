import { ConfigDefault } from "../"

export const FireElementAdditionConfig: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    range: "vuint[]",
    ratio: "vuint",
  },
}
