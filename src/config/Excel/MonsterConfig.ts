import { ConfigDefault } from "../"

export const MonsterConfig: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    boss: "string[]",
    monster: "string[]",
  },
}
