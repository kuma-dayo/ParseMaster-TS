import { ConfigDefault } from ".."

export const FlightDailyPointFactor: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    time_factor: "vuint",
    gold_factor: "vuint",
  },
}
