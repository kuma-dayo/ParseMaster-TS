import { ConfigDefault } from ".."

export const InferenceAction: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    type: "InferenceActionType",
    param: "string",
  },
}
