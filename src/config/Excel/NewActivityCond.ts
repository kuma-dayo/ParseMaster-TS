import { ConfigDefault } from "../"

export const NewActivityCond: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    type: "NewActivityCondType",
    param: "vint[]",
  },
}
