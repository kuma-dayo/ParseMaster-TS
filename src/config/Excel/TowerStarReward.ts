import { ConfigDefault } from "../"

export const TowerStarReward: ConfigDefault = {
  attribute: ["excel", "nozig"],
  baseClass: null,
  Fields: {
    minStarCount: "vuint",
    rewardId: "vuint",
  },
}
