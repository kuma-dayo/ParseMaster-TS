import { ConfigDefault } from "../"

export const ConfigGlobalMove: ConfigDefault = {
  attribute: [],
  baseClass: null,
  Fields: {
    syncInterval: "ConfigMoveSyncInterval",
    noGroundStayInPlace: "bool",
  },
}
