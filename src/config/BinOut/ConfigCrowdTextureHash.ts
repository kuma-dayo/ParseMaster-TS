import { ConfigDefault } from "../"

export const ConfigCrowdTextureHash: ConfigDefault = {
  attribute: [],
  baseClass: null,
  Fields: {
    dataQHash: "map<string,string>",
    dataTHash: "map<string,string>",
  },
}
