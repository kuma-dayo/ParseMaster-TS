import { ConfigDefault } from "../"

export const ConfigCanChangeAvatar: ConfigDefault = {
  attribute: [],
  baseClass: null,
  Fields: {
    normalizeStart: "float",
    normalizeEnd: "float",
  },
}
