import { ConfigDefault } from ".."

export const SkinColor: ConfigDefault = {
  attribute: [],
  baseClass: null,
  Fields: {
    renderer: "string",
    index: "vint",
    channel: "vint",
    color: "string",
  },
}
